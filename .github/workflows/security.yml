name: security

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  codeql:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Use GitHub CodeQL default setup
        run: echo "GitHub CodeQL default setup handles code scanning for this repository."
